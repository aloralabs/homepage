{
  "name": "aloralabs",
  "version": "1.0.0",
  "description": "Alora Labs company website",
  "license": "UNLICENSE",
  "scripts": {
    "build": "NF_IMAGE_VERSION=1 bash ./netlify-fix.sh && LD_LIBRARY_PATH=$(pwd)/tmp/usr/lib/x86_64-linux-gnu $(pwd)/tmp/usr/local/bin/hugo"
  },
  "dependencies": {
    "autotrack": "^2.4.1",
    "bootstrap": "^4.1.3",
    "bootstrap.native": "^2.0.23"
  }
}
